{% extends '@Game/base.html.twig' %}

{% block title %}
    {{ parent() }} :: Add new Race
{% endblock %}

{% block body %}
    <h1>Race creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('races_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
